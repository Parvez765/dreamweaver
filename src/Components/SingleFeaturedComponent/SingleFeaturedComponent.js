import React from 'react';

const SingleFeaturedComponent = ({project}) => {
    console.log(project);
    return (
        <div>
            
        </div>
    );
};

export default SingleFeaturedComponent;